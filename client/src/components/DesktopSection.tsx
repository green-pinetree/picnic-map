import React from 'react';
import styled from '@emotion/styled';

export default function DesktopSection() {
  return <Wrapper>DesktopSection</Wrapper>;
}

const Wrapper = styled.section`
  flex: 1;
`;
